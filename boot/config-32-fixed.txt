# For more options and information see
# http://rptl.io/configtxt
# Some settings may impact device functionality. See link above for details

# Enable I2C, SPI, and UART interfaces
dtparam=i2c_arm=on
dtparam=spi=on
enable_uart=1

# Enable audio
dtparam=audio=on

# Enable DRM VC4 V3D driver (required for graphics acceleration)
#dtoverlay=vc4-kms-v3d
max_framebuffers=2

# Disable firmware default video setup
disable_fw_kms_setup=1

# Run in 64-bit mode (if your Raspberry Pi supports it)
arm_64bit=1

# Disable overscan (remove black borders)
disable_overscan=1

# Run at maximum speed allowed by the board
arm_boost=1

# Waveshare 3.2inch display settings

dtoverlay=waveshare32b
hdmi_cvt=480 320 60 6 0 0 0
hdmi_group=2
hdmi_mode=1
hdmi_force_hotplug=1
hdmi_drive=2
